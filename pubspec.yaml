name: sidi
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  carousel_slider: ^5.1.2
  dio: ^5.9.2
  flutter:
    sdk: flutter
  get: ^4.7.3
  google_fonts: ^8.0.0
  intl: ^0.20.2
  location: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
